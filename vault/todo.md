---
type: reference
view: kanban
tags: [todo]
created: 2026-07-06
updated: 2026-08-19
---

Todo:

- [P3] WIKI-182 verdict archaeology — search across all archived reviewer verdicts. "Show every finding about mutation-not-load-bearing." Training data for future reviewer prompts + doctrine mining. Backend: full-text over archived transcripts. Frontend: search page w/ severity + author + date filters
- [P3] WIKI-183 cross-project search — grep across all orch vaults + PRs + commits + verdicts. Wiki + phoebe + tooling + misc in one query. Backend: multi-repo indexer (respect .gitignore); frontend: unified palette
- [P4] WIKI-184 voice steer — Henry says "restart WIKI-165" or "route WIKI-172 R1 to implementer"; app parses via speech-to-text + LLM intent extraction + routes. Overkill but cool. Depends on macOS speech API + safety confirm on destructive ops
- [P3] WIKI-185 auto-plan mode — given Linear-style problem statement, LLM decomposes into subtasks; each renders as workgraph node; orch can edit/reorder before spawning. Backend: planner endpoint. Frontend: plan editor
- [P3] WIKI-186 steer macros — reusable snippets in composer (reviewer prompt, mutation-verify contract, canonical-writer regression, iteration-cap check); one-click apply w/ ticket-name substitution. Cuts recurring prompt drafting
- [P3] WIKI-187 hot.md dedicated editor UI — arc-boundary rewrite surface: split-pane w/ live preview, section templates (Active threads / Recent facts / Watchouts), word-count budget indicator (≤500 target). Currently hand-edited via Read/Edit tools
- [P2] WIKI-177 blast radius view pre-spawn — PARKED 2026-07-30 by overnight orch after 7 review rounds: PR #147 open at 53ecaf7 + round-7 attestation refactor pushed; two structural false-clear blockers remain (blast_radius_cache.py:44 raw-tuple bypass; blast_radius_types.py:300 count-vs-identity fold — dropping an attestation passes all tests). Worktree .codex/worktrees/wiki-177-blast-radius + branch preserved. Resume = fresh implementer on those two items + REVIEW8, or Henry descope/close call
- [P1] backend: raise RLIMIT_NOFILE soft->hard at sidecar startup (GUI launch = 256 soft; wedge #7 defense-in-depth) — ESCALATED 2026-08-17: 10-worker fleet spike hit EBADF on event persistence, killed 3 live codex workers (WIKI-303/305/308 gen-1 replacements + a phoebe reviewer) and produced the WIKI-310 torn run.json; fd count normal (84) after spike, so the ceiling is the spike headroom
- wiki-native: respawn sidecar after signal exits (currently gives up after SIGKILL; needed manual app relaunch in wedge #7)
- check-aliveness: add daemon-mode sweep (registry tmux windows all NO-WINDOW for daemon-managed fleet; use /api/agents control_attached instead)
- [P2] WIKI-228 durable provider-health notice lifecycle — split from WIKI-154 R3 (orch scope call 2026-07-31): headless supervisor must track per-ticket Claude limit state and publish claude_limit_cleared on confirmed resumed-turn recovery (current clear event lives only in the legacy tmux watchdog, skips headless entries — notices persist forever); replace/archive flows must emit ticket-scoped resolution events consumed by account notices; disk-backed notice store needs a durable cursor/replay so provider failures during backend-closed windows survive reconnect (R3 findings 2+4, /tmp/WIKI-154-REVIEW3-verdict.json). Slot after 224/226 land (supervisor.py conflict)
- LC-6: integrate practice runner into roadmap app (misc, in progress, owner: misc orch)
- [P1] phoebe: PHO-15149 refactor umbrella to ORGANIZATION.md layout — ticket filed under epic PHO-15148; blocked by PHO-15082 (last parity PR)
- [P2] wiki backend: boot-sweep follow-ups from 2026-08-05 latency fix — skip terminal-state runs in orphan/costs sweeps + prune accumulated run-history store (multi-GB JSONL streaming still O(history) once per boot; related WIKI-227) + incremental/bounded checkpoint for costs state persistence (changed ticks serialize full history off-loop; deferred from WIKI-227 #180 R4 by orch ruling 2026-08-06)
- [P2] wiki-app: focus mode (cmd+.) — hide sidebar, transcript takes full window
- [P2] wiki-app: turn-level jump nav — j/k between turns, gg/G first/last, / filter
- [P2] wiki-app: sticky turn/event header while scrolling long turn
- [P3] wiki-app: peek-hover for [[wiki-link]] tokens (obsidian parity)
- [P3] wiki-app: diff-hunk anchor URLs from run summary → first hunk of a file
- [P3] wiki-app: session breadcrumbs (ticket → PR → round N → turn K)
- [P2] wiki-app: composer / command menu for orchestrator actions (spawn, steer, archive, gate, replace)
- [P3] WIKI-273 opencode2 provider kind (ocd) — add opencode v2 API-backed workers via @opencode-ai/client as a third kind alongside cc/cdx; additive adapter, NOT a refactor of native cc/cdx paths (decision 2026-08-10: preserve provider-native fidelity). BLOCKED until opencode2 API exits beta and contract stabilizes; watch https://opencode.ai/v2/docs/migrate-v1
- Re-land #13746 sandbox subagents after Henry's design pass (reverted 2026-08-11 in #14012; content preserved at 7c6d280551)
- [P3] wiki-app dashboard nit: duplicate stale label (WIKI-276 REVIEW4 LOW, non-blocking) — dedupe in a future polish pass
- phoebe: re-open PROMPTING.md registry consolidation PR from branch henry/consolidate-prompting-md-registry-updates after #14033/#14126/#14128/#14136 ship (needs Mason/Devaj codeowner approval)
- [P2] WIKI-284 dashboard needs-Henry lane — dedicated glanceable region for decisions only Henry can make: merge words pending, parked decisions (from vault/hot.md parked lists), iteration-cap surfacings, blocked-on-external items; each with one-line context + deep link; the decision queue becomes as glanceable as the fleet (from 2026-08-13 improvement discussion)
- [P2] WIKI-285 supervisor-owned review pipeline — mechanize the orchestrator rote loop the way WIKI-275 mechanized archiving: on implementer merge-ready, supervisor runs the gate (mergeable+threads+head) and auto-spawns the pinned reviewer per policy; orchestrator only routes verdicts and writes contracts; policy field at spawn like auto_archive (from 2026-08-13 improvement discussion)
- [P2] WIKI-286 auto-stage builds on merge — after each wiki merge, supervisor pre-stages a native build in the background (app-lock-free staging variant or queued until lock frees) so Henry's quit->swap is instant instead of quit->build->swap; autoswap watcher pattern becomes standing infrastructure (recurring friction: every arc ends with 'one more rebuild cycle')
- [P2] WIKI-287 phone access: push + one-tap decisions (DEPENDS ON WIKI-288 remote access foundation) — push notification when a decision blocks fleet progress (merge word, cap surfacing, parked decision) with deep link to the dashboard needs-Henry lane (WIKI-284); write-scoped v2 actions: one-tap merge word / resume / park from phone; transport candidates: ntfy/pushover vs APNs, auth story required before any write endpoint (Henry endorsed 2026-08-13)
- [P2] WIKI-288 remote access foundation — make the sidecar safely reachable off-box BEFORE any phone surface: (1) auth story (tailscale-first recommended: sidecar stays loopback, tailnet provides identity+encryption; fallback LAN bind + password like opencode's OPENCODE_SERVER_PASSWORD model), (2) publish an OpenAPI spec for the read endpoints (dashboard, agents, session) so clients can be generated, (3) explicit read-only vs write endpoint scoping so WIKI-287 write actions have a permission boundary to build on. WIKI-287 depends on this (decision 2026-08-13, from opencode gap analysis)
- phoebe: FILE LINEAR TICKET when token rotates — judgments table + query-tool read (Henry 2026-08-13, replaces #14128); worker already spawned as PHO-JUDGMENTS
- WIKI-291 [P2] wiki tui standalone mode — tui must not require Wiki.app: when the sidecar is unreachable, wiki tui boots the backend headless (same native_server entrypoint, no GUI, respects app.lock/supervisor.lock semantics so a later Wiki.app launch adopts or coexists cleanly) or falls back to read-only rendering from ~/.wiki/agent-runtime + /tmp/agent-status; depends on rebuild landing WIKI-276/290
- [P3] backend: pre-existing red on main — test_agent_runtime_store.py::ProtocolFixtureTests::test_codex_failed_render_completion_preserves_write_time_event fails on origin/main (1 failed / 1896 passed at 23c9cf2, 2026-08-13); triage whether env-dependent or a real regression; workers must cite it as pre-existing, not their baseline
- [P3] workgraph stall alarms: reset node stall counter on any worker activity (status-file write, push, steer receipt) not just node transitions — three false Henry-escalations during WIKI-282 (2062s/4314s/6069s) while the worker was actively implementing/gating; counter also survives replace_agent (known watchout); alarm should key on staleness of latest_event_at, not graph-node age
- [P3] backend flake: test_native_build_guard::test_transaction_stops_live_supervisor_before_taking_its_lock failed once under full suite on wiki-282-archive-parity @ 2780300 (2026-08-14), passed in isolation and on immediate full rerun — watch for recurrence; possible timing sensitivity vs watchdog under load; SECOND distinct flake same class 2026-08-14: DaemonProcessTests::test_fingerprint_swap_replaces_live_runs_under_fresh_daemon failed once in worker full run at aa831f1, passed direct rerun (orchestrator full run clean) — supervisor/daemon timing tests are load-sensitive as a family; ATTRIBUTION CORRECTED 2026-08-14 ~03:30: origin/main ALSO rotates failures under repeated full-suite load (1/3 baseline runs dirty: native_build_guard + implicit_diversity e2e) — branch attribution was WRONG (undersampled single-run baselines); the daemon/adapter/guard/e2e timing family is load-flaky on main itself. RAISE TO P2: deflake the family (event-loop timing tests need deterministic clocks/synchronization, not wall-time races); PR-4's in-memory telemetry fix kept as a good change regardless; family members observed so far: native_build_guard transaction-stops, daemon fingerprint-swap, daemon owns-fake-run, adapter background-rejection + working-resume, supervisor auth-dead-cap + r4-send-on-idle + recovery-resumes, e2e implicit-diversity, fleet-monitor earliest-replacement, and (2026-08-14) the new test_non_headless_session_keeps_native_parser from #228. REPRO PAIR FOUND (2026-08-14 ~16:45, gold for deflaking): running DaemonProcessTests::test_daemon_process_owns_fake_run_without_tmux then test_non_headless_session_keeps_native_parser fails ~2/3 of runs on BOTH main and feature branches (each passes alone) — deterministic-ish cross-test state pollution, start the investigation here. SMOKING GUN (2026-08-16, from WIKI-289 REVIEW19): test_wk_supervisor.py:238 uses a fixed asyncio.sleep(0.5) — the same fixed-sleep pattern is the likely mechanism across the whole family; grep for asyncio.sleep/time.sleep with fixed constants in the supervisor/daemon test fixtures. EXIT-HANG MECHANISM CONFIRMED (2026-08-14 ~19:30): suites COMPLETE then the interpreter hangs at exit joining a leaked multiprocessing resource (resource_tracker child alive, 0% cpu, summary trapped in the pipe; SIGTERM flushes it) — this caused every 'hung gate' incl. the 6h stall; per-test pytest-timeout cannot catch it (hang is post-suite); find the test that leaks the mp Queue/Pool
- phoebe: MORNING QUEUE for Henry — merge words needed as lanes green: #14313 (r4), #14312 (r2), #14287 (r5), #14254 (r4), #14259 (r4, merges only after #14126); leads still owe #14126/#14033/#14136 approvals
- phoebe MORNING NOTE (Henry): #14258 hit its 4th provenance boundary (Temporal dispatch/voice) — per escalation rule this was your decision point; I exercised your delegated judgment: round 4 kills the class at both ends (producer common-boundary restore + coordinator consults durable Outreach marker). If round 4 bounces again, the fallback option is scope-narrowing (exclude outreach-derived trigger types from this PR, follow-up ticket) — your call then.
- phoebe MORNING UPDATE (Henry): #14258 scope-narrowed per your delegated judgment — 5th provenance boundary found (agent-notes pipeline -> CaregiverAvailabilityUpdated); that trigger type removed from the PR, gated by new sub-issue PHO-15867; round 5 fixes the two small outreach items (voice call_ended restore, backstop limited to system-derived events so human acceptances still dispatch). Also filed PHO-15866 earlier (seek-switch follow-up for #14254).
- phoebe MORNING FINAL (Henry): six PRs need your merge words — #14287, #14312, #14254, #14258, #14313 (all dual-gated), #14259 (stack-gated, merges after #14126). Leads still owe #14126/#14033/#14136. Overnight merges: #14304/#14309/#14310. Follow-ups filed: PHO-15866 (seek switch), PHO-15867 (notes provenance). Fleet is empty.
- [P2] wiki backend: run.json torn write under load — WIKI-310 replace during backend congestion left run.json with a 32-byte garbage header (utf-8 decode error blocked every control call for that run until hand-rebuilt); make run.json persistence atomic (tmp+rename) and tolerant of partial reads (recovered copy at runs/a4930831*/run.json.corrupt, 2026-08-17)
- [P2] WIKI-341 wiki-app Terminal.app parity: bundle JetBrainsMonoNL .ttf via @font-face (font-display: block); add curated MONO_FONTS entries matching Henry's real installed family names (JetBrainsMonoNL Nerd Font Mono/Propo) so picker resolves instead of falling to SF Mono. Depends on WIKI-340 baseline. Design: [[terminal-parity-fonts]]
- [P2] WIKI-342 wiki-app Terminal.app parity: pixel discipline — integer line-heights on mono surfaces (line-height: <N>px not 1.5); audit and remove transform/filter/backdrop-filter/opacity<1/will-change/contain:paint above code blocks (compositor layer kills rendering fidelity); integer padding/margin on code containers; verify zoom: 1. Depends on WIKI-340 baseline. Design: [[terminal-parity-fonts]]
- [P3] WIKI-343 wk_cli test tighten — test_wk_cli.py:479 barrier test cannot detect if the readiness marker moves back above the add_reader calls. Wrap loop.add_reader to record fd registrations; assert both exist when the readers-ready callback fires. Follow-up LOW from WIKI-336-REVIEW7.
- [P1] WIKI-353 supervisor: backend restart wipes /tmp/agent-registry.json — whole fleet shows registered=false, orchestrator ids refuse spawns (Orchestrator id is not registered), sidebar loses grouping. Startup reconcile should rebuild registry entries from runs/*/run.json with the correct entry schema (orch/ticket/kind/cwd/log/session_id/spawned_at/transcript — raw run.json keys do not resolve). Manual repair playbook in [[orchestrator-worker-protocol]] (2026-08-19)
- [P2] WIKI-354 supervisor: deterministic uuid5 run ids collide with committed archives of prior incarnations — archive_agent fails with 'older archive marker cannot remove a newer live run' for any respawned orchestrator/ticket whose earlier run was archived. Archive resolution should key on (run_id, created_at) or run ids should salt with spawn time. Manual repair (re-id old archive artifacts) in [[orchestrator-worker-protocol]] (2026-08-19)
- [P2] WIKI-355 supervisor: registry-driven recovery spawns an uncontrolled provider for dead runs — a reseeded/stale registry entry makes recovery start a fresh provider (gen+1) whose control channel never attaches; POST /stop refuses ('provider PID is live without attached control; refusing false stop') leaving a zombie that needs manual SIGTERM. Recovery must attach control or transition the run dead; stop needs a force path for unattached pids (2026-08-19, tooling orch revival)
- [P1] WIKI-357 supervisor: move archive finalize (export, copies, fsyncs, deletion, registry projection) off the event loop to a bounded archive worker thread; loop side keeps state admission + completion publication. Test: 1000-event archive with concurrent ping loop staying <3s. Rank-1 fix from [[supervisor-timeout-diagnosis-2026-08-19]] (archive_current holds RunStore._lock across the whole operation, store.py:1867-2012; measured 2.6-9s)
- [P1] WIKI-358 supervisor: move the synchronous event-persistence bundle (JSONL fsyncs, run.json writes, reducer + SQLite materialize) to a bounded writer executor; preserve per-run order via the existing event lock; persistence failures return to the event pump. Measure p50/p95/p99 materializer latency. Rank-2 fix from [[supervisor-timeout-diagnosis-2026-08-19]] (_dual_write_normalized blocks the loop, supervisor.py:628-699)
- [P1] WIKI-359 supervisor: drop full run_is_healthy() validation from the archive/export hot path — use the durable cursor + committed marker for normal export; full parity validation moves to the background backfill path with an explicit repair path. Rank-3 fix from [[supervisor-timeout-diagnosis-2026-08-19]] (health check on 635MB db measured 6.3-9.1s, event_store.py:1819-1849)
- [P2] WIKI-360 supervisor: recovery scans must tolerate a run disappearing after list_runs() — skip RunNotFound for that run without a full traceback; removes archive/recovery race noise (supervisor.log RunNotFound spam q1s from daemon.py:106-118 recovery loop). Rank-7 low-effort fix from [[supervisor-timeout-diagnosis-2026-08-19]]
- [P1] WIKI-372 supervisor: unix-socket command listener dies under load while the process survives — ConnectionRefused on supervisor.sock (accept loop crashed?), HTTP reads unaffected, whole fleet unspawnable until app relaunch. Hit 2026-08-19 ~16:20 EDT during the fsync-storm window. Likely an unhandled exception in the socket accept task on the single loop — same family as [[supervisor-timeout-diagnosis-2026-08-19]] (WIKI-357/358); the accept loop needs supervision/restart and a crash log line

Silky-smooth artifact rendering arc (Henry 2026-07-29):

- [P2] WIKI-196 artifact hover-preview strip — hovering a chat message with N artifacts shows a mini-strip preview (thumbnail row); click any = expand inline; ambient discovery without click-in
- [P2] WIKI-197 drag-drop composer — drag images/files/folders from Finder into composer; auto-uploads as artifact + embeds as `![]()` or `[[]]` reference. Files >5MB warn before send
- [P2] WIKI-198 file-list peek + side-pane — hover on kind:file-list entry shows peek (first 20 lines or thumbnail); click → opens side-pane w/ full viewer instead of external app hop
- [P2] WIKI-199 universal copy/share/download surface — every artifact has consistent header actions (copy raw, copy image/svg, download, share URL if backend exposes). Currently per-kind ad-hoc
- [P3] WIKI-202 image/PDF OCR + searchable — OCR pass on receive (tesseract or macOS Vision framework); text index searchable via vault + palette. Enables grep over screenshot/PDF content

Unknown provider-stream renderer arc (Henry 2026-07-29 — audit of `disposition=unknown` events across last 50 runs; see backend/app/agent_runtime/normalizer.py classifier):

- [P2] PHO-13800 customer_churned_date timing unreliable (19/29 in one entry window; zero churns Jan-Apr) — backfill true dates or document entry-date semantic; related PHO-13735
- [P2] PHO-13763 PARKED by Henry 2026-07-15: PR 11383 drafted at 909ef288ce, gate-passed (CI green, 0 threads, sol REVIEW4 merge-ready), worker archived — resume = un-draft, re-verify freshness vs main, merge
- [P3] WIKI-126: surface rebrand — rename app-facing identity only (app display name, window title, README header); NAME NOT YET CHOSEN by Henry, blocked until he picks. Internal identifiers stay (repo, wiki CLI, WIKI-* tickets, MCP names, vault paths — codename doctrine, Henry 2026-07-15)
- [P2] amd-shadow invalid index: KORGAN owns fix (PR 11416 closed w/ evidence); prod index still INVALID 0-byte — watch deploys
- [P2] PHO-13826/27/28 exe.dev sandbox arc: enable (SSH key secret + smoke) -> ownership fix -> TTL sweeper; PHO-13829 egress design backlog. Surface merged since PHO-13073/#10608, dormant on missing key
- [P3] wiki backend /metrics endpoint (gauge follow-up)
- [P3] pufferclone /metrics endpoint (gauge follow-up)
- Slack admin agent access for Corey Grissom — confirm with Corey his @phoebe.work email + provisioning plan, then either (a) have him sign into app.phoebe.work with @phoebe.work Google Workspace so Kratos provisions the user row + flip admin=true, or (b) one-time hotfix INSERT app.users row + backfill org_slack_user_mappings.user_id for slack_user_id U0AGES89BLZ (2 org rows: Phoebe Home Care + Orchard St. Homecare, both currently user_id=null). Root cause: no app.users row exists at all — his Slack identity is known but unmapped. Full diagnostic + proposed SQL in phoebe session 2026-07-17.
- [P1] PHO-14029 prod dedupe backfill (bun run people:dedupe-backfill --write) + PHO-14033 unique index migration on people(lower(email)) — EOD 2026-07-22
- [P1] WIKI-158 global resilience + lifecycle states: first run, backend down, provider auth, update available, notices — coherent first-run path, backend outage != empty vault, persistent sign-in state, all states announce success recovery
- [P2] WIKI-159 keyboard + dialog accessibility: kanban/dashboard filters/destructive dialog/context menu — keyboard equivalents for drag/double-click, listbox+menu+dialog semantics complete, focus trap+restore, destructive copy describes outcome+recovery
- [P2] WIKI-160 design-token convergence + shared controls: spacing/radii/motion/icons/shadows, settings+status components — one spacing/radius/motion vocabulary, no dup radius aliases, theme-token shadows, weights cap 600, primitives everywhere (rebase after WIKI-144)
- phoebe: fix mock.calls[0] assertion bug in use_scratchpad_index.test.ts round-trip tests (rode into #12058 per Henry merge call)
- [P3] wiki backend: 2 pre-existing auth-dead timing tests fail on hosts with uptime <3600s (monotonic threshold; found by WIKI-166 REVIEW1 full-suite run 07-27) — make uptime-independent or skip-guard

In Progress:

- [P2] WIKI-181 reviewer diversity harness — spawn N reviewers w/ distinct lenses (correctness / security / perf / test-strength) in parallel; synthesize verdicts. Codified adversarial verify — one lens catches what another misses. Wire into next_review (WIKI-171) as opt-in mode — cdx:WIKI-181

- [PR #10475](https://github.com/phoebe-health/phoebe/pull/10475): subagent recommendation parity iteration (harness #10692) — cdx:PR-10475 worker; overfitting watch
- [P?] [PHO-13274](https://linear.app/phoebework/issue/PHO-13274): post account-health automation as per-owner Phoebe Slack threads with sales-call + product-agent context — cdx:PHO-13274 worker
- [P3] [PHO-13367](https://linear.app/phoebework/issue/PHO-13367): fix Slack admin agent shifts-filled-through-Phoebe miscount (scope to callout, dedupe unique shift) — cdx:PHO-13367 worker
- [P4] [PHO-13368](https://linear.app/phoebework/issue/PHO-13368): admin agent run events monospace font — cc:PHO-13368 worker
- [P2] [PHO-13669](https://linear.app/phoebework/issue/PHO-13669): Core email/account-book gaps — cdx:PHO-13669 (gpt-5.6-terra)
- PHO-13763 per-org scratchpad (worker cdx:PHO-13763, run bd90b506)
- [P2] PHO-13830 live EHR record fetch tool (admin agent) — worker live
- [P2] PHO-13832 shift classification + calendar artifact (admin agent) — worker live
- [P2] PHO-13826 ModalSandboxBackend + PHO-13827 sandbox ownership — workers live (Modal replaces exe.dev for v0; 13828 lifecycle + 13829 egress design queued)
- mitmweb rebuild: scope and build a clearer live proxy-traffic inspector — owner (misc); merged through B6 (tooling PR #10, 2026-07-21); remaining: P1 packaging
- WIKI-135 dashboard: implementation workers only (drop reviewers/one-shots) — owner cdx:WIKI-135 (luna)
- [P2] WIKI-191 audio artifact kind — new `kind: audio` inline w/ waveform preview + scrubber + speed control; transcript overlay if attached. For voice memos, TTS output, transcription evidence
- [P1] PHO-14864 land agent-bash-recs-proto on main behind feature flag (owner: phoebe orch)
- [P1] [PHO-14963](https://linear.app/phoebework/issue/PHO-14963): implement provider-neutral v3 tool discovery and Anthropic delivery — cdx:PHO-14963
- [P1] [PHO-14975](https://linear.app/phoebework/issue/PHO-14975): implement v3 write registry and diff-first flow — cdx:PHO-14975
- [P1] [PHO-14977](https://linear.app/phoebework/issue/PHO-14977): implement v3 read-only helpers and eval scaffolding — cdx:PHO-14977
- PHO-15151 bash rewrite spec — worker spawned, PR to main (owner: phoebe-dev)
- PHO-15110 schema derivation prototype — worker spawned, PR to umbrella, held until #13193
- PHO-15171 v3 local TUI — worker spawned, PR to umbrella (owner: phoebe-dev)
- PHO-0-DEEPWIKI admin deepwiki investigation — research worker, report to /tmp/PHO-0-DEEPWIKI-report.md
- PHO-15206 chronic recommendation test fixes — worker spawned, PR to umbrella
- PHO-14522 orphan cleanup fix — worker spawned, PR to main
- PHO-14037 on-call transfer — investigate-then-fix worker spawned
- PHO-15253 bash rewrite PRs A/B/C — night shift worker
- PHO-15254 retrieve adoption tranche 1 — night shift worker
- [P1] [PHO-15373](https://linear.app/phoebework/issue/PHO-15373): make Bash a permanent v3 core tool — cdx:PHO-15373
- [P1] [PHO-12744](https://linear.app/phoebework/issue/PHO-12744): cap WellSky read timeout and total retry wall-time — cdx:PHO-12744
- [P1] [PHO-15375](https://linear.app/phoebework/issue/PHO-15375): prevent activity related-entity query timeout — cdx:PHO-15375
- [P1] [PHO-15632](https://linear.app/phoebework/issue/PHO-15632): v3 coordinator phoebe-event triggers with shared legacy primitives, caps, and picker — owner cdx:PHO-15632
- [P2] newt PHYSICS ARC (owner: tooling orch): recreate MuJoCo from scratch in Rust. v0-v3 COMPLETE + major feature surface (PRs #45-#74: dynamics, contacts incl hfield, all solvers/integrators, exact impedance, tendons+wrap, all actuators incl muscles, sensors, MJCF, differential harness, perf baseline NEWT-30). Spec: newt/docs/superpowers/specs/2026-08-13-newt-physics-design.md. NEWT-32 perf pass 2 MERGED (PR #77). Remaining queue: analytic derivatives / SDF / mesh-mesh / native-CCD alignment (NOTE: unmerged pre-reset branch newt-convex-ccd + .codex/worktrees/newt-32 hold mesh-contact-frame WIP from 2026-08-17). Open findings: native-CCD box contacts, RK4 constraint-ZOH
- [P2] WIKI-289 wk harness v0 — wiki-native minimal agent harness built ON the Claude Agent SDK (plan-authed, NO api credits — Henry constraint 2026-08-13: same billing path as cc workers; raw provider SDKs ruled out): default tools stripped, our own read/write/edit/bash + integrity tool layer, our system prompt, emits wiki-native events targeting the WIKI-282 store; codex lane IN v0 per Henry 2026-08-13b: thin wk driver over codex app-server/exec under plan auth (only plan-authed path; protocol known from WIKI-266) — both lanes emit the same wiki-native event format, harness-level integrity tools shared; design MUST study https://github.com/earendil-works/pi as the reference architecture (Henry 2026-08-13c) — agent loop shape, session trees, compaction, tool interface, RPC mode; steal patterns, not code; LAUNCH FEATURE FLAG required (Henry 2026-08-13d): wk is opt-in at app/sidecar launch (flag/env, e.g. WIKI_ENABLE_WK=1), DEFAULT OFF — without the flag wiki launches byte-identical to today, no wk worker kind offered, no wk codepaths active: DESIGN-FIRST, then reviewer-lane experiment ONLY (wk reviewers head-to-head vs cdx sol, 2-week quality+cost comparison); harness-enforced integrity (gate runner captures real exit codes, mutation runs recorded by tool layer, status writes by the loop — workers physically cannot misreport); cc/cdx remain first-class worker kinds, coexistence not replacement, expansion gated on measured results; slot after WIKI-282 lands (Henry approved direction 2026-08-13); CODE PHASE COMPLETE 2026-08-16: PRs 1-5/6 merged (#229 3r core+flag, #230 4r claude lane, #231 5r codex lane, #233 3r supervisor+ingest, #234 5r integrity: ledger-authoritative status, false-ready guard forgery-proven, causal replay). PR 6/6 = OPERATIONAL: run the 2-week reviewer experiment — BLOCKED ON: (1) rebuild (quit->stage->swap, queue #214-#234), (2) Henry launching with WIKI_ENABLE_WK=1 (opt-in, default off). Experiment per locked decisions: claude lane first, ~30 reviews/lane, 10% non-inferiority vs cdx sol, plan-usage-unit cost report; expansion gated on the report
- [P1] [PHO-15860](https://linear.app/phoebework/issue/PHO-15860/bound-codebase-wiki-reader-cache-and-split-the-module): pr 2 codebase-wiki module split — owner cdx:PHO-15860-PR2
- [P2] WIKI-336: wk terminal chat driver — ./wk REPL over the wk lanes (claude/codex), one-shot -p mode
- [P1] WIKI-337: session view renders every user message twice (sqlite reducer feeds ignored stdin echoes to the parser) — fix + regression tests
- [P1] ZETA-1 zeta: core types + ConversationStore (append-only JSONL, parent links, replay, torn-tail repair) + AgentLoop skeleton w/ fake backend + tests — repo ~/me/fun/zeta, design docs/design.md
Backlog:

- [PHO-14974](https://linear.app/phoebework/issue/PHO-14974): implement the planned v3 describe catalog after retrieve and write registry interfaces land
- [PHO-14976](https://linear.app/phoebework/issue/PHO-14976): implement the planned V3 parity batches from the 123-row inventory after registry interfaces stabilize
- [PHO-11469](https://linear.app/phoebework/issue/PHO-11469): re-triage Slack-thread alert investigation
- [PHO-12425](https://linear.app/phoebework/issue/PHO-12425), 12426: sandboxed outreach start + e2e dogfood
- [PHO-11256](https://linear.app/phoebework/issue/PHO-11256)–11260: texQL series
- [PHO-11269](https://linear.app/phoebework/issue/PHO-11269)–11272: canonical data products series
- [PHO-11274](https://linear.app/phoebework/issue/PHO-11274), 11275: trace comparison + prompt/context diffing
- [PHO-11251](https://linear.app/phoebework/issue/PHO-11251), 11264, 11267: artifact actions, playbook authoring, known-issue memory
- [PHO-12211](https://linear.app/phoebework/issue/PHO-12211): vendor record-and-inject primitive for sandbox probes
- [PHO-11598](https://linear.app/phoebework/issue/PHO-11598): Snowflake QA/feedback + trends tool
- [PHO-12134](https://linear.app/phoebework/issue/PHO-12134): Voice QA inspection support
- [PHO-11727](https://linear.app/phoebework/issue/PHO-11727): subagents as nested tasks in /admin/agent
- [PHO-11539](https://linear.app/phoebework/issue/PHO-11539): admin agent as read-only MCP server
- [PHO-11540](https://linear.app/phoebework/issue/PHO-11540): daily run review summary for tool quality
- [PHO-11651](https://linear.app/phoebework/issue/PHO-11651), 11629: playbook input validation + progress events
- [PHO-11535](https://linear.app/phoebework/issue/PHO-11535): fact/inference evidence tiers for RCA answers
- [PHO-11231](https://linear.app/phoebework/issue/PHO-11231): Phoebe Home Care seed data for visual testing
- [PHO-12982](https://linear.app/phoebework/issue/PHO-12982), 12757: reference tickets (harness doctrine, tool brainstorm)
- [P1] [PHO-13138](https://linear.app/phoebework/issue/PHO-13138): Phase 2 admin-owned tables — plan approved (ticket comment = contract, 8-PR series); cdx:PHO-13138 worker on PR1 (schema/roles/deny-tests)
