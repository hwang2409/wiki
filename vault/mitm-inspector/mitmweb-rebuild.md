---
type: campaign
tags: [mitmproxy, proxy, frontend]
created: 2026-07-16
updated: 2026-07-29
---

# Mitmweb Rebuild

Build a clearer, high-density local inspector for live mitmproxy HTTP(S) flows, starting with Anthropic reverse-proxy traffic.

## Locked MVP

| In | Out |
|---|---|
| Loopback-only reverse proxy; live virtualized flow grid; paired request/response inspector; JSON/text/SSE/hex views; timing; native-filter-compatible search | Replay/edit/intercept controls; remote access; raw persistence/export; TCP/UDP/DNS; cloud/team features |
| Public-hook capture addon; versioned local IPC/API; redaction before transport; bounded memory retention | Forking mitmweb; consuming its private REST/WebSocket API; storing raw `Flow` objects |

- Working path/name: `~/me/fun/misc/mitm-inspector` / `mitm-inspector` until release naming.
- Default retention: memory only, newest 2,000 completed flows or 30 minutes, 128 MiB global body budget, 1 MiB captured prefix per side.
- Sensitive headers/query values are irreversibly redacted before leaving the proxy process; bodies remain sensitive and appear only on selection.
- UI: virtualized 28px flow grid over paired inspector; follow-live pauses when navigating history; request/response/error lifecycle ordering is not assumed.
- Delivery graph: `S0 → {B1 runtime, B2 capture, F1 shell, F3 inspector} → {B3 API, F2 flow grid} → I1 integration/security/perf → {P1 packaging, F4 restyle}`.
- F4 restyle merged via `e0f474b` (candidate `7da535b`, four review rounds cdx/luna implement + cdx/sol review): wiki-app design language ported across `tokens.css` (paper/ink/teal/amber/danger palette + radii + typography), `shell.css` (dense responsive layout), `flows.css` (28px virtualized grid with muted grid lines), `inspector.css` (paired detail with responsive stacking at ≤800px viewport width). Real-browser responsive pin lives at `web/tests/inspector_responsive.py` (Playwright + chromium, probed at 600/800/1024/1440 with row-height and viewport-fit assertions); wired into `npm test` via `test:e2e` that bootstraps `npm ci` + `playwright install chromium` on a fresh env with `--strictPort` + port-availability + `proc.poll()` guards so port collisions and stale servers fail loudly instead of silently passing. Round-1 flagged HIGH 76px inspector clip @800px + MEDIUM dead tokens; Round-2 flagged HIGH jsdom geometry vacuous; Round-3 flagged BLOCKING playwright undeclared + BLOCKING fixed-port collision; Round-4 clean, every mutation verified. Merged main passes 388 pytest, ruff, mypy strict pkg-scope, tsc, eslint, 149 vitest, and the four-width e2e probe on Python 3.12.
- F5 minimalist follow-up merged via `ba80c53` (candidate `874280f`, three review rounds cc/opus-4.7 implement + cdx/sol review): black/white/grey light-default palette with explicit allowlist grep test covering `tokens.css` + `index.html` (chroma leaks fail); red reserved for 4xx/5xx errors only; JSON auto-parse tree (`web/src/features/inspector/jsonTree.tsx`) with collapsible nodes and monochrome key/string weights; SSE frame streaming that preserves the truncated trailing suffix in both render and copy; per-request summary line with Anthropic `/v1/messages` special-case (`POST v1/messages · <model> · <n> msgs · <bytes in> / <bytes out> · <duration> · <status>`) and always-both-slots generic fallback (`<method> <path> · <status_or_—> · <duration_or_—>`); flow-grid new/seen state via `useSeenFlows` hook hoisted above the workspace so seen bits survive reconnect and prune against retention/source IDs across the full 2,000-flow window; response_status range 100–599 enforced in JSON schema, Python adapter/protocol, and TS parser with pytest+vitest pins; error/in-flight metadata paths gated to omit `response_status`; `SSE ▸` glyph in the flow grid streaming column. Round-1 flagged 1 HIGH (seen-IDs unmount on reconnect) + 4 MEDIUM (SSE zero-frame fallback, generic summary spec drift, palette theme-color+redaction leak, response_status inconsistency) + 1 LOW (missing `▸`); Round-2 flagged 5 MEDIUM (seen-history 1024 cap silently rolled over vs 2000 retention, SSE truncated-suffix drop, summary missing status slot, palette.test.ts blanket-trusted tokens.css, error metadata not gated to null status); Round-3 clean. Merged main passes 388 pytest, ruff, mypy strict pkg-scope, tsc, eslint, 196 vitest, and the four-width e2e probe. Live proxy runnable via `mitm-inspector/Makefile` (`make up` = install + backend :8000 + reverse proxy :8080 + vite dev :5173; `make up-prod` for preview mode) — committed (16e3f34 annotated targets + test alias merged into gate).

## State

Planning complete from supervisor runs `MITMWEB-ARCH`, `MITMWEB-INGEST`, `MITMWEB-UX`, and `MITMWEB-SCOPE`. Foundation S0 merged locally at `420f2e2`. B1 runtime merged at `6214643` after five independent review rounds. F3 standalone paired inspector merged via `7e78199` after six rounds. F1 connection client, source/cursor state machine, follow-live shell, reconnect/resync UX, dense responsive styling, and mounted DOM coverage then merged via `fd9e251` after six rounds. B2 capture/store merged via `712df98` (candidate `9a1d1dc`) after fifteen review rounds — capture adapter/sequencer/gate/sink with atomic ownership handoffs, durable drains, bounded retention, and released-flow accounting guard; merged main passes the full pytest suite, ruff, and strict mypy on Python 3.12 (sequencer intentionally rejects 3.13 — `uv sync --python 3.12`). Wave 1 complete. F2 flow grid merged via `b0dee31` (candidate `8c99d3c`, one review round): virtualized 28px grid, mitmproxy-native-compatible filter language, follow-live/selection workspace over the F3 inspector, bounded 512x32 browser retention; 133 vitest plus Playwright desktop/mobile verification. Four LOW findings deferred to I1: flow-limit boundary test not mutation-pinned, per-event deep-refreeze O(512x32) hot path needs live-load tuning, `~t` matches parameter-stripped content type, inspector lacks a lifecycle-truncation marker. B3 API merged via `2bef0c8` (candidate `21e98c7`, two review rounds): loopback-only HTTP+WebSocket protocol-v1 delivery (snapshot/delta/cursor/gap/resync), body-redacted grid projection, strict JSONL unix-socket ingest, readiness probes, live CLI wiring; Review1's MEDIUM (close() hang on live connections under Python 3.12 wait_closed semantics) fixed and empirically verified. Wave 2 complete — every foundational ticket is merged. I1 integration merged via `8e73838` (candidate `fe2b8e7`, three review rounds, cdx/luna implement + cdx/sol review after fable session-limit + credit exhaustion caused a mid-flight replacement): proxy-side `CaptureSocketPump` addon draining the shared sink to the runtime's private ingest socket with per-message backpressure and reconnect/backoff loss accounting; real `/api/v1/stream` WebSocket transport with JSON framing, resync sends, and full-detail fetch via `/api/v1/flows/{id}`; incremental `browserState` retention that reuses frozen retained state and preserves `truncatedFlowIds` under pruning; incremental API projection per `flow.metadata` ingest matching full re-projection semantics; envelope accounting that reserves worst-case JSON-escape overhead and rejects oversized headers before serialization while tolerating surrogateescaped bytes; `close()` no-latch behavior so start→close→start round-trips serve traffic. Sol Round 1 flagged 2 BLOCKING (non-load-bearing loss-accounting and close-race tests) + 2 HIGH (unbounded pre-drain pump buffer, envelope cap under-counts headers) + 2 MEDIUM (fast-path bypass hidden, off-by-one truncation banner); Round 2 flagged 1 HIGH (envelope missed JSON escaping / surrogateescape) + 2 MEDIUM (pruning dropped truncation markers, `_closing` latch); Round 3 clean, all round-1/2 mutations still fail their pins. Merged main passes 388 pytest, ruff, mypy (package scope), tsc, eslint, and 148 vitest tests on Python 3.12. Architecture follows [[public-hook-adapter]]. Verified against [mitmproxy 12.2.3](https://github.com/mitmproxy/mitmproxy/releases/tag/v12.2.3), [event hooks](https://docs.mitmproxy.org/stable/api/events.html), and [Anthropic streaming semantics](https://platform.claude.com/docs/en/build-with-claude/streaming).

Readability arc (2026-07-20, Henry-driven): all `/v1/messages` rows render identically and the JSON tree buries conversation content — trial-and-error packet hunting. Decision: approach 2 — backend-derived per-flow summary + search over sqlite bodies + frontend conversation view, with a NO-INFORMATION-LOSS invariant (conversation view is a re-arrangement of the JSON, never a reduction; raw tree always one toggle away). Wire contract (additive optional FlowMetadata fields): started_at/ended_at, body sizes, content types, content_encoding marker (responses stored gzip — served decoded from now on), and `summary` {kind msg/cnt/generic, model, message_count, preview (last human user text, system-reminder-stripped, 140ch; tool_result-only turns render as tool_result + tool name), token/stop_reason fields}; plus `GET /api/v1/search?q=` (case-insensitive substring over durable request bodies + decodable response bodies, snippets ≤160ch). Shipped as MITMWEB-B4 (backend, tooling PR #2) + MITMWEB-F7 (frontend rows/search/conversation view, tooling PR #3), merged 2026-07-20 — first PR-based merges on the tooling monorepo; cross-PR contract converged over 3 joint rounds (search matches carry optional sanitized `flow` projection; delta order = canonical newest-first, unknown-ID upserts block-prepended preserving batch order, pinned in docs/api.md; durable-only search hits open via sqlite detail fallback).

S1 durable SQLite persistence merged via `07f2b53` (candidate `bfd092a`, eight sol review rounds): restart replay, bounded DB+WAL+SHM retention, runtime storage controls, private-file/path policy, crash-reopen reclamation, non-destructive invalid-cap rejection, and independently pinned exact byte boundary. The clean merge tree passes 577 pytest, 207 vitest, ruff, mypy, typecheck, and eslint.

F8 session-first UX merged via tooling PR #5 (squash `cd1430b`, 2026-07-21, Henry merge auth): session-list home page (one metadata-only row per session: start time, initial query, flow count, models, error flag; suggestion-mode previews excluded from firstQuery), session drill-in chat rendered from a canonical main-thread request + its streamed response, sanitized markdown (images rendered inert — URL/title visible, zero fetch), and a manual source picker (transient override, canonical-first ordering, raw-inspector fallback for unparseable candidates, popover keyboard semantics). Canonical selection (`web/src/features/sessions/canonical.ts`) is the load-bearing core: prefix chains over normalized histories (cache_control stripped at block top level; balanced complete `<system-reminder>` injection tolerance with linear tokenized validation), non-consuming shared-root components with member-to-component union indexing, distinct-stage deep-equality evidence aggregated per equivalent-history group, strictly-growing lineage with request-context identity (model+system+tools), equal-length prefix-equivalent clone guard, context ownership with deterministic group representatives, two-phase order-aggregate refresh — incremental==fresh pinned by randomized equivalence (14k rounds clean at review 12). Verified empirical basis: consecutive main-thread requests are exact prefixes modulo cache_control movement + appended reminder blocks; sessions mix main thread with suggestion-mode and 1-message utility calls; final assistant turn lives in the response body. 12 sol review rounds (worker lineage: cdx/luna spawned, replaced by cc/fable-5 per frontend rule, killed twice — AUP content-filter false positive from XSS payload strings in status text, then Claude credit exhaustion — finished as cdx/luna until codex credits also died; rounds 12-13 findings fixed by the orchestrator directly with in-session review fallback + 300k-trial differential fuzz). Gotchas codified: never paste exploit-style payload strings into agent status/chat text (API content filter kills the session); stateful incremental indexes must be discarded on mid-update throw or memoized fast-paths serve stale state.

Readability/perf burst merged 2026-07-21 (tooling PRs #6-#10, all squash):

- **F9 readability polish** (PR #6 `b117f92`, cc/fable-5 + /frontend-design + /make-interfaces-feel-better; Henry verdict on v0 was "super dense, unreadable"): presentation-only over `SessionList.tsx` + `shell.css` — two-line session rows (query as primary line, dot-separated meta line), bordered ERR chip, centered `.conv` column (960px max, 76ch measure, 13px/1.65 prose), role-distinct turn cards, thinking-block left rule. Survived one supervisor-restart worker respawn with worktree takeover.
- **F10 chat-UI overhaul** (PR #7 `7ed5ef6`): session drill-in reads as a human/LLM back-and-forth, not a packet dump. Henry kill-list applied: AUXILIARY CALLS collapse, side-call meta line + packet tables, "no main-thread conversation" card, default-visible source picker (now behind `···`, multi-candidate sessions only). Breadcrumb header, 1120px shell, 860px transcript column, make-interfaces-feel-better polish pass.
- **F11 chat render fidelity + wider layout** (PR #8 `dbe26bc`, driven by Henry's real-traffic session): transcript widened to `clamp(960px, 92vw, 1320px)` with 76ch prose measure preserved; `remark-gfm` (tables/strikethrough/task-lists/autolinks); untrusted captured tags (`<system-reminder>` etc.) escaped by rewriting mdast `html` nodes to `text` — visible verbatim, no HTML reaches the DOM; zebra GFM tables + labeled tool_use/tool_result code cards. 211 vitest.
- **B5 backend read-path perf** (PR #9 `a5a6cb4`): detail reads resolve through the memory-store flow index instead of scanning/sorting all retained messages; skip re-validating already-validated internal messages. Measured: 11.6s → 17.8ms (2 flows); 30s+ timeouts → <100ms (24/27 flows). Audit at `mitm-inspector/docs/b5-audit.md`. 621 pytest, 216 vitest.
- **B6 websocket browse path removed** (PR #10 `dac2808`): WS stream/subscriber fanout/cursor state/reconnecting transport deleted; sqlite-backed `GET /api/v1/sessions` (+`?limit&before`) and `GET /api/v1/sessions/<id>`; production SPA reads over HTTP. Redacted projections preserved. 197 vitest.

Remaining: P1 packaging. Repo housekeeping done 2026-07-29: merged worktree `.worktrees/mitmweb-f9` removed, stale merged remote branches (b4/b5/f7) pruned.
