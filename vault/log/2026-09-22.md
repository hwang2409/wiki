---
type: log
tags: [log, jev]
created: 2026-09-22
---

# 2026-09-22 — jev orchestrator handoff

## State

- Repo: `~/me/fun/jev` (private, hwang2409/jev). Master at `40c6fc4`. Zero open PRs. No live workers. No active worktrees.
- Arc 4 (safety tier, "yolo seatbelt") is MERGED. PR #5 squash `40c6fc4`. Details in [[typesafe-jev]].
- Vault is current: `hot.md` rewritten at this boundary, `done.md` logged, note updated.

## What merged

Fail-closed safety gate for shell commands in the harness fork:

- Layer-0 classifies each command: DENY, ESCALATE, or ANALYZABLE.
- Auto-approve is an allowlist privilege. Only a positively-parsed simple command can auto-approve after a benign Jev score.
- Un-parseable structure escalates to a human prompt by construction.
- Jev errors, timeouts, and a missing key deny. Positive danger-Nouls block auto-approve.
- `_LAYER0_RULES` reason taxonomy is enforced by tests. 429 targeted tests. Telemetry rides the production late-sink path.
- Flag: tier is OFF by default. Off-mode has a byte-identity test.

Six review rounds. Locked lesson: enumeration of dangerous shell shapes never converges. The burden of proof sits on the command.

## Pending Henry decisions

1. Python ergonomics: every `python3 script.py` escalates (exec-capable interpreter). Python-heavy work sees frequent prompts with the tier on. A carve-out would reopen a bypass surface.
2. Default-on for the safety tier and for memory injection. Wants eval data first.
3. Parked from before: phase-3 sim eval needs a real ANTHROPIC_API_KEY (placeholder at ~/.zshrc:279); iMessage tool designed, unbuilt; `.pem` workspace-carveout asymmetry (round-5 nit, documented).

## Queued work (in order)

1. Labeled command-corpus acceptance eval + live smoke for the safety tier.
2. Arc 3: Jev-navigated browsing (Playwright toolset, element-level routing).
3. Big-catalog routing crossover eval (bridge to arc 3).
4. JEV-30 docs-sweep refactor; 3 realistic-eval task calibrations (see todo.md).

## How to resume the lane process

- Orch id `jev`. Spawn via HTTP `POST 127.0.0.1:8213/api/agents/spawn` (include `"orch":"jev"`; archive outcome must be `closed|merged|abandoned` — `failed` 422s).
- Implement workers: cdx gpt-5.6-luna high. Reviews: codex REFUSES adversarial safety-review contracts (cyberPolicy flag) — use cc opus-4.7 with an authorization-context preamble. Implement contracts pass codex fine.
- PR flow: worktree branch `jev-<n>-<slug>`, PR titled `JEV-<n>: ...`, orch squash-merges on a clean review pass. Next ticket number: JEV-60.
- Workers write `/tmp/agent-status/<TICKET>.json`; the file-first monitor (task bwhgnfqts) reads `/tmp/agent-status/.jev-watchlist` (currently empty).
- Capture reviewer verdicts and worker attestations from `~/.wiki/agent-runtime/runs/<run_id>/raw.jsonl` after a 60s mtime quiesce, THEN archive.
- Standing rule: everything stays in the experimental fork. Nothing ports to real zeta until Henry decides. Do not re-raise.

## Sixth full Wiki.app reset (~11:51 EDT, Henry's ask)

- Repo ff-checked against origin/main (already at tip eafbf175); local vault edits preserved.
- Killed Wiki.app + sidecar + supervisor (SIGKILL needed). Live fleet died: orchs jev/zeta/feebies, codex workers JEV-65, ZETA-142, PHO-17613-F8.
- Wiped: knowledge.db (9.7 GB, FIRST deletion) + .semantic, command-log.sqlite3*, metadata.sqlite3*, agent-runtime/runs/, workgraphs/, /tmp/agent-status, /tmp/agent-registry.json, stale locks. Kept agent-archive, cost-aggregation, logs.
- Rebuilt via `ALLOW_MISSING_APP_LOCK=1 make native-build` (swap transaction verified daemon), relaunched; backend healthy on 127.0.0.1:8213.
