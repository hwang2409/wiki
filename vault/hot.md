---
type: reference
tags: [hot]
created: 2026-07-06
updated: 2026-07-23
---

# Hot Context

Rolling ≤500-word session cache. Rewrite (don't append) at work-arc boundaries: what changed, active threads, watchouts. Injected into every Claude session at start.

## Active threads

- **TOOLING MONOREPO LIVE 2026-07-20**: all 4 misc subprojects (`mitm-inspector/`, `pufferclone/`, `tix/`, `gauge/`) migrated to `github.com/hwang2409/tooling` (private) via subtree, per-project history preserved. Local at `~/me/fun/tooling`. All mitm work now PR-based against origin/main; worker worktrees under `.worktrees/<ticket>`.
- **MITMWEB CURRENT (tooling orch)**: B4+F7 merged 2026-07-20 (PRs #2, #3 — flow-summary enrichment, durable body search, conversation view). S1 durable sqlite merged. **F8 session-first UX MERGED 2026-07-21** (PR #5 squash `cd1430b`): session-list home (metadata-only rows), canonical main-thread chat drill-in (`canonical.ts` prefix chains, incremental==fresh pinned 14k randomized rounds), manual source picker w/ raw fallback, sanitized markdown (inert images). 12 sol rounds + orchestrator-fixed rounds 12-13 under dual credit exhaustion (in-session review fallback + 300k differential fuzz). **F9 readability polish IN FLIGHT** (cc/fable-5 + /frontend-design + /make-interfaces-feel-better; Henry: v0 "super dense, unreadable"; presentation-only). Full arc [[mitmweb-rebuild]]. Remaining after F9: P1 packaging; Makefile still uncommitted awaiting sign-off.
- **CODEX RE-AUTHED 2026-07-22 ~20:37Z** (Henry, new limits after brief exhaustion at 20:34Z): all live cdx workers replaced on fresh tokens per protocol (14258/14285/14272/14250 + 14260-REVIEW1 back to sol). cdx pipeline normal.
- **FABLE-5 WORKING AGAIN 2026-07-21** (orchestrators + F9 run on it; watch for credit blocks recurring — resets seen Jul 22 11am ET Claude / Jul 24 11:24pm codex). Frontend rule: frontend/UI tickets get cc fable-5 + mandatory /frontend-design (+ /make-interfaces-feel-better for polish tickets) — codified in [[orchestrator-worker-protocol]].
- **Misc clone arc** (2026-07-16): pufferclone v1+v2 (PUF-8..12, main@6bc5d64), `tix` (TIX-1), `gauge` TSDB v0 (GAU-1..5, master@9d32f0b). Next clone candidates in [[local-cloud]]: Temporal-lite vs LSM KV — Henry pending. Unticketed: cargo install tix/puf/gauge, tix vault migration, PUF-13 NVMe tier.
- **Wiki 2026-07-22 QUAD MERGE (wiki orch)**: WIKI-148 composer slash menu (#119@33c951a, 9 rounds — runtime remote ACL for get_wiki_app_secret, modifier-Enter single-dispatch, busy-cancel semantics), WIKI-151 nav+sidebar IA (#124@fdf77f0, 3 rounds — explicit workspace-unavailable state, file-scoped retry nonce, harness ui-state isolation fixed at wrapper level), WIKI-156 artifact shell (#125@316e015, 3 rounds — shared loading/error/fallback surface across normal/compact/panel, session-scoped expand state, artifact-renderers extraction), WIKI-161 synthetic-source messages (#123@c58b319, 4 rounds — send_now source field, system-row rendering, dedupe-rollback hardening, Codex-shape unread policy). Henry gate outstanding: manual Wiki.app /spawn verify (WIKI-148) at next sidecar swap. **Regression sweep 07-22 eve (3db94ae, inline on main, no PR per Henry)**: sidebar orch->worker grouping restored (kept #124 Active/History + row anatomy), Cmd-K palette artifact-cache (14s/keystroke -> 0.2s; events.jsonl mtime cache + startup warm), non-whitelisted markers visible again (killed "run detail" chips), synthetic-row padding/wrap, composer echo-lag dedupe (live claude transcripts never echo pending_id; fallback window now sent_at..echoed_at — was duplicating every typed + steered message). SIDECAR SWAPPED + relaunched by Henry 07-22 ~22:38Z — quad merge + sweep live, Henry: "everything looks great" (WIKI-148 /spawn gate presumably satisfiable now). Sweep included 335c4c9 (sidebar dots dropped per Henry, unread reads via bright ticket text; owned rows indent 27px). **GRAPH-ENGINEERING D1+D2 MERGED overnight 07-22/23 (wiki orch)**: WIKI-162 D1 schemas+linter (#126 squash c06678f, 5 cdx-luna rounds — steer-path Steer validation CLI+MCP, nested SHA binding, frozen-sidecar schema bundling caught by round-2 review + real PyInstaller MCP smoke, per-error required-key pointers) and WIKI-163 D2 workgraph+renderer (#127 squash fd92d5f, 5 cc-fable-5 rounds — canonical spawn/steer/archive append typed edges via keyed outbox, snapshot-before-hot commit + orphan reconciliation under per-ticket flock, (mode,request_id) replay idempotency w/ additive edge-schema property, lifespan-drained outbox, injected clock, /agents/<T>/graph live+replay routes). Review loop rhythm: verdict-route -> archive reviewer -> replace implementer w/ contract file each round; findings narrowed 4/2/2/1/clean (D1) and 6/4/4/2/clean (D2). Follow-ups filed: WIKI-165 (P2, FleetMonitor graph_health integration, deferred from D2 round-1), P3 crash-test gaps (schema-invalid orphan, two-crash convergence), LOW spec-spelling test brittleness. **D3 ALSO MERGED 07-23 ~04:11Z** (WIKI-164 #128 squash 43c80b3, 2 rounds — 6 doctrine templates, template schema wired into wiki graph lint, `wiki graph select-template` ticket-prefix resolver with repo.implement fallback; trigger-DSL deferred). Full graph-engineering D1->D3 arc complete. Also fixed inline on main: 01d7642 — D2 shipped order-dependent test failures (outbox deliveries racing tmpdir cleanup via tearDown-before-addCleanup ordering; provider-health lifespan tests leaving global outbox closed), caught by WIKI-164's full-suite run, 728-test suite green after. Fable-5 session limit re-exhausted during D2 rounds 3-5 (resets 11pm ET; 3 stream-drop replace/nudges). Landed post-swap sidecar debt: 93f41ff + 44ff01c (readable col 910px) + cf9732b (mono-theme diff colors) + c06678f + fd92d5f — relaunch picks up all. Open: daemon-ize backend, WIKI-126 rebrand blocked on name, compact-mermaid-preview unfiled, WIKI-94/109 flake candidates (WIKI-92 contract re-anchored in #125). Review-gap monitor false-positive class: implementer mid-fix post-verdict looks "merge-ready, no reviewer" — proposed suppress-15m-after-verdict-route tweak pending Henry. **WIKI-165 D2-followup MERGED 07-27 (#131 squash 137bd45, 4 rounds)**: FleetMonitor graph_health integration — composite-health detector (blocking>0 + no live reviewer q5m re-alarm subsumes review-gap; stall>1800s escalation edge; iteration_count>cap escalation), typed escalation appends through sole writer w/ ack-on-success + retry, snapshot-fallback on invalid hot graphs + `graph-unavailable` q5m alarm, latest-verdict-tied suppression, backward-clock cadence reset + `0<=route_age<900` bound, durable episode markers (survive monitor restart), centralized role normalization (fleet+writer via `ticket.py` — AUDIT/CANARY/THERMO/EVAL covered), 680→633 line fleet_monitor.py via `graph_health.py` extraction (527 lines). Canonical end-to-end restart regression (no mocks). R1-R4 review arc: 1B+4H+1M → 5H/M → 1H → clean. **WIKI-163-P3 MERGED 07-27 (#130 squash 977bd6b, 3 rounds)**: workgraph orphan-fallback loop bounded (first schema-valid candidate is authoritative, prior loop scanned all history), divergent-newest regression made load-bearing (r3 spawn `request_id` differs from hot), bounded-read + invalid-newest-orphan-fallback + two-crash-convergence tests added. Full graph-engineering + D2-followup arc CLOSED.
- **Phoebe tool-discovery arc 07-22/23**: PHO-14285 framework prerequisite MERGED (#12095 squash 62bc583a, 3 rounds). PHO-14286 eval harness IN FLIGHT (#12142): round-1 review caught HOLLOW harness (fabricated corpus provenance vs prod run tables, protocol-only replays, inverted P95/P99 guardrails); round-2 rebuilt non-provider parts (truthful corpus via logfire, percentile fix, join validation) but hosted replays BLOCKED on Henry gate: `bazel run //evals:cloud -- auth store` (no cloud-eval credential on machine; token env var = EVAL_PLATFORM_API_TOKEN, keychain-backed via evals/cloud_auth.py). Then PHO-14287 wiring -> 14288 rollout.
- **Phoebe agent-first /admin epic (PHO-14243) + fleet night 07-22/23**: MERGED tonight: #12095 (14285), #12105 (14272 worker handoff + 48h rollup, 4 rounds), #12112 (14260 haiku tier: prose classifier KILLED for server-validated mechanical_contract, ~67.3% cost cut on mechanical map subagents, 5 rounds). PARKED ON HENRY: #12058 (14238 scratchpad UI, cap 8/8 done, one-line test fix: mock.calls[0] w/o clearing navigateMock — round-9-vs-orchestrator-patch decision) and #12066 (14247 dock shell, cap 8/8 done, two test edits: initialChatId+chat_id assert in image probe; A/B stream instances keyed at surface mount — same decision; dock screenshots on PR since 01:17Z, Henry visual sign-off still pending). #12103 (14250 chat-vs-page doctrine) round 5: Bazel CI red at fbeb70beaa, worker fixing new 60-64KB override regression test; Henry SIGNED OFF user_overrode field on apply_linear_workflow_mutation (recorded in auto-memory). After 14247+14250 merge: PHO-14248 chip integration (unblocks on dock), PHO-14249 audit trail (parallel, Henry nod pending). PHO-14329 filed (predictable tool failures -> next-step hints, from run 019f8bee RCA: artifact_ref lacked retrieval affordance). Corey admin-agent access FIXED (admin flag was fine; org_slack_user_mappings row for Phoebe Home Care was NULL — one-row prod UPDATE, Henry-approved).
- **Phoebe** (phoebe orch, RCA-driven ops 07-18/19 — 5 merges): 07-19: PHO-14060 exception-noise cleanup MERGED (#11763 squash 236463b2, 1 clean sol round — wellsky api_host_forbidden + rollback-observability skips no longer log exc_info, kills ~2.5k fake exceptions/day). PHO-14061 agent-correctable admin tool validation errors MERGED (#11764 squash b9371707, 4 sol rounds — boundary ValidationError->corrective field messages, provider schema honesty for Any/null, fields-set presence). 07-18: PHO-14053 wellsky clock-out (#11747 0fae1222, 4 rounds), PHO-14047 DRI auto-join (#11745 946ba0c9), PHO-14046 retry observability (#11744 a72d5435). RECURRING: logfire 30-min sweep loop ARMED (session cron e720f044, 11,41 * * * *, auto-RCA + spawn-if-urgent, known-noise baseline in prompt). OPEN GATES (Henry): PROD DEPLOY GAP — last deploy 07-18 19:09Z @ 946ba0c9; #11747/#11763/#11764 + others undeployed, deploys manual/scheduled not merge-triggered. Avondale HHAX -9 GetPatientDeclinedCaregivers CS escalation (client sync dead since 07-14; optional code fix unticketed). Green Tree voicemail-transfer = CS config (ivr+post_dial_digits recipe, org on nonFixedVoip/Sinch; no code). Sign-in circular-JSON RCA parked (posthog empty, sentry CLI unauthed). PHO-14023 gallery pick, PHO-14029 prod dedupe apply, checkout disposition all still pending. rca skill at .claude/skills/rca/SKILL.md (uncommitted, works well). Fleet doctrine addition: fold adjacent cases into open PR (no dud PRs); quiet parked merge-ready re-alarms by clearing watchlist row.
- mitm-inspector requires Python 3.12 — sequencer intentionally rejects 3.13; `uv sync --python 3.12` in every new worktree (uv defaults to 3.13).
- mitm merge flow: local repo, no remote/PR — merge --no-ff into main, gates on merged main, remove worktrees, delete branch.
- Reviewer state field unreliable — verdict step text authoritative, always.
- Steer mode default `now` (Henry); on-idle only for genuinely-next-task input.
- Fleet monitor doctrine (Henry): watchlist file + re-alarms (verdict q5m, review-gap q10m, staleness 30m); never rebuild monitors per spawn/archive.
- `wiki` CLI not on monitor-shell PATH — use `~/me/fun/wiki/wiki` absolute.
- Supervisor MCP ops can 1s-timeout under load — verify effect before retrying steers. Soft cap 5 workers; above it expect provider timeouts.
- Workers survive backend restarts; status file `/tmp/agent-status/<T>.json` readable when backend down.
- cdx-only gotchas (dormant until quota reset): plugin-install elicitation hangs turns — ban in spawn prompts; cyberPolicy kills "adversarial/attack"-phrased review prompts — phrase "correctness/robustness"; startup hangs — replace_agent; after token re-auth replace every live cdx worker.
- Phoebe: BuildBuddy ExecuteWorkflow reruns check out raw branch (no main merge) — for fixes-on-main, merge origin/main + push. Bazel cache can hide time-bomb tests — unchanged-target red → suspect main. Worker test policy (Henry): no full local suites, directly-touched targets only, CI authoritative.

## Watchouts

- NEVER paste exploit-style payload strings (script URLs, XSS probes) into agent status text, steers, or chat — Anthropic AUP filter kills the session unrecoverably (poisoned context; replace_agent only fix). Payloads live in test files only. Bit MITMWEB-F8 2026-07-21.
- Wiki backend flapped repeatedly 2026-07-21 ~10:30-10:50 ET (app swap): supervisor restart mass-archives workers as handoff and respawns orchestrators — check `list_agents` after any restart, respawn lost workers with takeover kickoffs (uncommitted worktree edits survive). Vault rolled back once in the same window — verify vault notes against git/GitHub reality after restarts.
- Next free ticket ID: WIKI-169 (165 = FleetMonitor graph_health MERGED 07-27; 166 = workgraph sidecar phantoms MERGED 07-27; 163-P3 crash-test gaps MERGED 07-27; 167 = compact-mermaid preview; 168 = daemon-ize backend). WIKI-146/147/150 verified done + pruned 07-27 (palette, unread, census all in main).
- Mastermind merge-ready loop cap raised 3→8 (see [[feedback_merge_ready_iteration_cap]]) — gpt-5.6-luna needs more rounds; auto-steer unless plateau on same finding for 3 straight rounds.
- Pin review worktrees + gates to exact SHA; archive reviewer immediately after verdict routed.
- Workers' claims verified by mutation — keep ordering it in review prompts.
- Local main can hold unpushed vault commits — pull --rebase before push.
- `.codex/worktrees/` ~40 stale entries — prune pending Henry.
- CI polling: use `gh_pr_watch.py --watch` (babysit-pr script), NEVER bash `until [ "$(gh pr checks ... | awk ...)" = "pass," ]` — awk/sort predicate silently misses pending→green (bit us on PHO-14003 #11688 2026-07-17).
- Bazel slot pool (/tmp/bazel-slots, 2 slots): bazel server JVMs HOLD the slot lock FD until shutdown — archived reviewers' servers become zombies starving the pool (bit us hard 07-23; freed via `bazel shutdown` in the stale worktree). Run `bazel shutdown` in review worktrees before removing them. Push-hook contention has Henry's --no-verify exception (slot contention only).
- cdx workers idle-wait on already-green CI instead of polling — 4 instances 07-22/23; kickoffs must say "poll gh pr checks actively, do not idle-wait", nudge on staleness alarm.
- Steer cap: `steer_agent.text` max_length = 4000 chars — long multi-finding steers hit ValidationError. Write full detail to `/tmp/<ticket>-review<n>-findings.md`, send terse steer pointing at file path (bit us on MITMWEB-F5 REVIEW1 2026-07-17).
