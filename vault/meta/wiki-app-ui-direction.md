---
type: decision
tags: [meta, wiki-app, frontend]
created: 2026-07-06
updated: 2026-07-08
---

# Wiki App UI Direction

Decision (2026-07-06): the wiki frontend is an **Obsidian clone in structure/behavior, monochrome black-and-white in appearance**. Light mode default, light/dark switcher in ribbon (persisted to `localStorage["wiki-theme"]`).

## Chosen

- Obsidian layout: ribbon, file-explorer sidebar, tab header, breadcrumbs, reading/source mode toggle, status bar with word/char count.
- Markdown parity with Obsidian core: callouts (all 13 families, foldable `+`/`-`), `[[wikilinks]]` + aliases, `==highlights==` (rendered inverted fg/bg), `#tags` (gray pills), `%%comments%%` hidden, footnotes, task lists, tables, syntax highlighting (grayscale weights/italics, not colors).
- Monochrome palette via CSS vars on `:root` (light) + `[data-theme="dark"]` overrides in `frontend/src/styles.css`. Callouts differ by icon only, not color.
- Backend full-text search: `GET /api/notes?q=`.
- Custom views, frontmatter-gated: `view: kanban` renders sections as read-only board lanes, list items as cards (used by `todo.md`). First deliberate departure from pure markdown parity.
- Kanban target look: obsidian-community/obsidian-kanban plugin — multi-column lanes (width proportional to card count, internal card grid; single-column tried and reverted same day), panel lanes on flat pane, rich markdown cards. Shipped 2026-07-06: drag-drop between/within lanes + per-lane add-card, both write todo.md via line surgery (exact-line ops on latest content, last-write-wins). Drop-to-done shipped: drag card onto floating zone → line appended to log/done.md (`- YYYY-MM-DD: <text>`, priority tag stripped) + removed from todo.md; done-append happens first so failures can't lose the card. Remaining: card edit-in-place, lane menus.
- Split panes (2026-07-06): recursive binary split tree, unlimited panes. Every pane shows a 5-zone overlay while dragging a file from the tree — edge drop splits that pane, center drop replaces its note; closing a pane lifts its sibling; dividers drag-resize each split (15–85% clamp). Secondary panes are read-view + full kanban interactivity, ephemeral (not in URL). Cmd+B toggles sidebar; Cmd+K quick switcher.

## Rejected

- **Per-element CSS customizer prototype** (`wiki-styles` fenced blocks, style inspector, media placement controls) — removed entirely; legacy blocks stripped at render. Do not reintroduce.
- **Obsidian default color theme** (purple accent `hsl(254,80%,68%)`, per-type callout colors) — replaced same day by monochrome at Henry's request.

## Key files

- `frontend/src/markdown.tsx` — renderer + callout/wikilink/tag/highlight plugins
- `frontend/src/App.tsx` — shell, theme toggle
- `frontend/src/styles.css` — both theme palettes
- `vault/meta/ui-demo.md` — exercises every renderer feature

## Known gaps (deliberate, "expand later")

`![[embeds]]` render as links not transclusions; reading-view checkboxes not clickable; no math/mermaid/graph/backlinks.

Locked 2026-07-06: plain-textarea editor is deliberate and permanent — the wiki is read-first for Henry; agents write the notes. No CodeMirror/live-preview investment. Shipped same day: link index (`GET /api/links` — outgoing/incoming/unresolved per note; Linked-mentions section in reading view) and the `wiki` CLI (repo root — todo add/move/complete, log-done, note new with map update, lint sweep). Option 3 locked: CLI for hot paths + lint detection; raw markdown stays sanctioned for prose. Also shipped: activity feed (`#/activity`, git-log-backed — day-grouped vault commits, file pills, expandable diffs, 8s refresh) and graph view (`#/graph`, canvas force layout — degree-sized nodes, hover neighborhood, drag, click-to-open, unresolved ghost nodes). Both on the ribbon. Batch 2 same day: kanban card edit-in-place (dbl-click), SSE live push (`/api/events` mtime+git-refs watcher — replaced all polls), vault-health page (`#/health`, staleness buckets from frontmatter `updated`), and `vault/hot.md` rolling session cache w/ global SessionStart injection hook. Batch 3: file ops (shared `backend/app/vaultops.py` — smart rename rewrites wikilinks code-aware + map.md, hard delete prunes map; `wiki note rename/delete`; tree right-click menu, drag-file-to-folder move, click-unresolved-wikilink → create dialog). Activity merged-Shipped layer built then reverted same day — Henry prefers the feed as pure commit stream; done.md stays the separate ledger.
- `/agents` page (2026-07-07, expanded 2026-07-08): live worker fleet monitor. Push registry (`wiki agent register/done`, `/tmp/agent-registry.json`) + worker status files + tmux liveness → cards w/ state badge, role/kind/model chips, step, blocker, staleness, handoff-chain history, pane-log tail. Spawn-from-UI shipped: `Spawn worker` modal on `/agents` posts to `POST /api/agents/spawn`, validates uppercase ticket/kind/role/model/effort/workdir/orch/prompt, writes `/tmp/<kind>-<TICKET>-prompt.md`, spawns tmux worker window + pane log, registers via `wiki agent register`, then relies on existing SSE refresh for the new card. Steering from UI still v2 candidate. Schema in [[orchestrator-worker-protocol]].
- Agent PR review surface (2026-07-08): live worker cards with a PR show a `Review` affordance; the sidebar and `#/agent/<TICKET>` view both expose a review tab backed by `GET /api/agents/{ticket}/pr` and `POST /api/agents/{ticket}/pr/approve`. The panel stays monochrome and reuses the existing split-hunk diff renderer from activity, surfacing checks, review decision, unresolved threads, and inline approve/refetch so Henry can gate a worker PR without leaving the app.
- Session sidebar (2026-07-07, Conductor-style): log button → right sidebar, Session + Terminal tabs. Session tab renders the worker's NATIVE transcript (`backend/app/transcripts.py` — codex `~/.codex/sessions` rollout JSONL + claude `~/.claude/projects` JSONL → normalized events: user/assistant/thinking/tool w/ call-id output joins, incremental byte-offset parse cache). Session↔ticket mapping: kickoff prompt `Linear ticket X` regex + `spawned_at` disambiguation (codex), project-dir slug (claude). Rendering: assistant markdown, user prompts collapsed, consecutive tool+thinking grouped into collapsible "N tool calls · M thinking" rows. Terminal tab = `tmux capture-pane -p -J` for live windows (tmux renders redraws — killed all spinner-residue ANSI cleanup pain), pane-log file cleanup fallback for dead ones. Polling: 2.5s client interval only while sidebar open + SSE tick, delta protocol (`?after=` cursor, server rewinds to oldest pending tool call so output joins land; memoized React blocks → only tail re-renders). Tool calls classified into ~10 archetypes (read/search/edit/git/github/validate/wait/status/ticket/infra — empirical census of 31 codex sessions) → icon + human summary per row (`read SKILL.md:1-240`, `status → working`), raw io on expand. Transcript formats are unversioned CLI internals — expect drift, parsers defensive. Archived workers (same day, 2026-07-07): `/api/agents` lists last 20 from `~/me/fun/agent-archive` (final-status.json state/pr, role parsed from kickoff prompt); same sidebar — native transcript resolved via archive-timestamp hint, archived pane log as fallback when transcript cleaned up. `wiki agent done` now persists `{outcome, ended_at, worker snapshot}` into archive `meta.json` (order: archive BEFORE done; `wiki agent outcome <T> merged` retro-fixes); outcome chip on archived cards. Left sidebar has files/search/agents modes (ribbon toggle) — agents mode lists live+archived workers, click → `#/agent/<TICKET>` full-screen session view (820px measure, deep-linkable). Three surfaces share one transcript renderer (`session.tsx`). Settings modal (ribbon gear, `settings.tsx`): monospace font picker (JetBrains Mono default; Geist/Fira/Plex/Source/system — all fontsource-bundled, browser downloads only the selected) + note/UI font-size sliders driving the CSS vars, all localStorage-persisted and applied at boot. Multi-orchestrator (same day): `wiki agent orch <ID>` self-registers a Claude session as orchestrator — reads `$CLAUDE_CODE_SESSION_ID` for the EXACT transcript path (no discovery guessing); workers registered `--orch <ID>` group under it in sidebar + /agents; orch row click renders the orchestrator's own claude transcript (adapter verified live). Registry `_orchestrators` key; `orch-done` at session end. Orchestrator-native event renderers: `<task-notification>` → bell rows, slash-command invocations → command rows, system-reminder wrappers stripped; archetypes steer (tmux send-keys w/ STEER var parse), monitor (Monitor/TaskStop), agent (Agent/Skill), ask (AskUserQuestion), mcp__* prefix stripping for ticket tools. Browser→agent input: composer under session views (Enter = send-keys now; Shift+Enter = on-idle queue in `/tmp/wiki-msg-queue.json`, backend dispatcher delivers when pane spinner absent ×2) + minimal vim mode in composer (Esc normal: h l 0 $ w b x dd i a A I). Claude subagents: `<session-dir>/subagents/agent-*.jsonl`, Agent tool rows annotated by prompt-head match → `inspect` chip → read-only right panel; running tools pulse. Multi-view: agent runs open in split panes (drag sidebar row to pane edge, `agent://<id>` layout paths); tmux prefix Ctrl+A then l/h/0-9 cycles agent runs (orch=0, workers 1..n, indices shown in sidebar).
